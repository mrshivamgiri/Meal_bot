{
  "permissions": {
    "allow": [
      "Bash(docker compose ps)",
      "Bash(docker compose ps *)",
      "Bash(docker compose logs)",
      "Bash(docker compose logs *)",
      "Bash(docker compose config --no-interpolate)"
    ]
  }
}
